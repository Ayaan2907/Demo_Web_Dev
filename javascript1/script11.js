console.log(" hello arrow functions");
{
      var abeySaale = function (param) {
            return `${param} kootriyaa...!`
      };
      console.log(abeySaale('tameezSe'));
      name1 = (params) => ` ${params} is here`
      console.log(name1('kaif'));
}

const myArray = [
      {
            title: 'Wake up',
            description: 'wake up early',
            done: true,
      },

      {
            title: 'Pray fajar',
            done: true,

      },

      {
            title: 'go to walk',
            done: false,
      },

      {
            title: ' get ready for college',
            description: 'do it fast',
            done: false,
      }
];

{// methods for outputting
// const displayMyArrayTrueOnlyTitle = myArray.filter(workList => workList.done === true).map(workList => workList.title);
// const  displayMyArrayFalseObject = myArray.filter(workList => workList.done === false);

// console.log( `titles of done items are [${displayMyArrayTrueOnlyTitle }] and objects which are not done are ${displayMyArrayFalseObject}`); 
};
// console.log(myArray.filter(workList => workList.done === true).map(workList => workList.title), myArray.filter(workList => workList.done === false))

console.log(VarDate)